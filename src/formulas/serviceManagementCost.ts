/** Service management and CR fee per submission (USD). */
export const serviceManagementPerSubmissionUsd = 3.73;

/** Service management and CR fee per page (USD). */
export const serviceManagementPerPageUsd = 0.0365;
